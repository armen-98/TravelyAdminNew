NEXT_PUBLIC_API_URL=http://localhost:8000/api
# Used for absolute URLs in metadata (OG image, canonical). No trailing slash.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
AUTH_SECRET="replace-with-long-random-string"
